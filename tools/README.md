# Tools

Build, codegen, lint, and release scripts that are project-wide rather than tied to a single workspace member.

## Planned contents

- OpenAPI codegen scripts (REST clients + types)
- GraphQL schema diff tooling
- Plugin manifest validator
- Theme contract test runner pipeline
- Release tooling (changelog, sigstore signing)
- WP migration test corpus generator

## Status

Empty — content added as needed during P0 and P1.
