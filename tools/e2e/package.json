{
  "name": "@gonext/e2e",
  "version": "0.0.0",
  "private": true,
  "description": "GoNext end-to-end test harness. Playwright runs against the docker-compose stack. See docs/11-testing-ci.md §11.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:list": "playwright test --list",
    "install-browsers": "playwright install --with-deps",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "@types/node": "^20.11.0",
    "typescript": "^5.4.0"
  }
}
