{
  "name": "@gonext/blocks-sdk",
  "version": "0.0.0",
  "private": false,
  "description": "Block-editor SDK for GoNext. Type definitions, JSON Schema validation, registry, and migration helpers for the block tree. Used by the editor, the renderer, themes, and plugins. Apache-2.0.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src",
    "README.md"
  ],
  "scripts": {
    "build": "echo '@gonext/blocks-sdk: source-only package, no build step'",
    "lint": "echo '@gonext/blocks-sdk lint: not yet implemented'",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ajv": "^8.17.0",
    "ajv-formats": "^3.0.1"
  },
  "devDependencies": {
    "@gonext/test-config": "workspace:*",
    "@vitest/coverage-v8": "^1.6.0",
    "typescript": "^5.6.0",
    "vitest": "^1.6.0"
  }
}
