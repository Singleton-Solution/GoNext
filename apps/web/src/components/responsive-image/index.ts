// Re-export so callers do
//   import { ResponsiveImage } from '@/components/responsive-image';
// rather than reaching into the file path.
export { ResponsiveImage, buildWidthSrcSet } from './ResponsiveImage';
export type { ResponsiveImageProps, PictureSource } from './ResponsiveImage';
