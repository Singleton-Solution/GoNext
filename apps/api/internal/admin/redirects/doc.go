// Package redirects exposes the admin REST surface for redirect rule
// administration. The routes are documented in docs/01-core-cms.md;
// this file's job is to wire packages/go/redirects.Store + .Engine to
// HTTP handlers the admin UI consumes.
package redirects
