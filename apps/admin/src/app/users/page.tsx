/**
 * Users — placeholder.
 *
 * The real user-management surface (list, invite, role assignment) lands
 * in a follow-up issue.
 */
import type { ReactElement } from 'react';

export default function UsersPage(): ReactElement {
  return (
    <section>
      <h1>Users</h1>
      <p className="muted">Users list — placeholder.</p>
    </section>
  );
}
