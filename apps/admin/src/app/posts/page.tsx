/**
 * Posts list — placeholder.
 *
 * The real CRUD list with table, filters, and bulk actions ships in
 * issue #31.
 */
import type { ReactElement } from 'react';

export default function PostsPage(): ReactElement {
  return (
    <section>
      <h1>Posts</h1>
      <p className="muted">
        Posts list — placeholder. The real listing arrives in issue #31.
      </p>
    </section>
  );
}
