/**
 * Settings — placeholder.
 *
 * Real site/identity/integration settings ship in a follow-up issue.
 */
import type { ReactElement } from 'react';

export default function SettingsPage(): ReactElement {
  return (
    <section>
      <h1>Settings</h1>
      <p className="muted">Settings — placeholder.</p>
    </section>
  );
}
