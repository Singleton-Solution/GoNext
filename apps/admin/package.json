{
  "name": "@gonext/admin",
  "version": "0.0.0",
  "private": true,
  "description": "GoNext admin dashboard. Next.js (SPA-style routing). Hosts the block editor, site editor, and all CRUD UIs. Status: skeleton — issue #1 only adds the workspace entry.",
  "scripts": {
    "build": "echo '@gonext/admin build: not yet implemented'",
    "dev": "echo '@gonext/admin dev: not yet implemented'",
    "lint": "echo '@gonext/admin lint: not yet implemented'",
    "test": "echo '@gonext/admin test: not yet implemented'",
    "typecheck": "echo '@gonext/admin typecheck: not yet implemented'"
  }
}
